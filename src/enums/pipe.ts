/**
 * Enum for reserved method names that cannot be used in pipe expressions.
 * @enum {'applyFilter' | 'sort' | 'pipe' | 'items' | 'count'}
 */
export enum ReservedMethodsEnum {
  APPLY_FILTER = 'applyFilter',
  SORT = 'sort',
  PIPE = 'pipe',
  ITEMS = 'items',
  COUNT = 'count',
}
