/**
 * Enum for string boolean operations.
 * Used to specify boolean checks on strings (empty/not empty).
 * @enum {'is_empty' | 'is_not_empty'}
 */
export enum StringBooleanOperEnum {
  IS_EMPTY = 'is_empty',
  IS_NOT_EMPTY = 'is_not_empty',
}
